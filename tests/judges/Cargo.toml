[package]
name = "ptterm-judges"
version = "0.1.0"
edition = "2021"
description = "Read a screen back from other terminal emulators, to compare against ptterm"
license = "MIT"

[dependencies]
alacritty_terminal = "0.26"
wezterm-term = { git = "https://github.com/wezterm/wezterm" }
serde_json = "1"

[profile.release]
debug = false
